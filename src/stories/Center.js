import React from 'react';
const styles = {width: '100%', height: '100%',  display: 'flex', justifyContent: 'center', alignItems: 'center'}

export default ({children}) => <div style={styles}> {children} </div>